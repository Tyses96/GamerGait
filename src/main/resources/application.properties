spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=changemeinprod!
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update

gamerGait.icon-url=res/GamerGait.png

server.ssl.enabled=true

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=src/main/resources/keystore/gamergait.p12
# The password used to generate the certificate
server.ssl.key-store-password=Atriox@96

server.port = 8443

# frontend stuff
frontend.server.ip=localhost
frontend.server.port=443
frontend.server.protocol=https

